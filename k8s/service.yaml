apiVersion: v1
kind: Service
metadata:
  name: vibe-kanban
  namespace: vibe-kanban
  labels:
    app: vibe-kanban
spec:
  selector:
    app: vibe-kanban
  ports:
    - name: http
      port: 5174
      targetPort: http
